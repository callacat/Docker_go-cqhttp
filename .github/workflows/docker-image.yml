name: Docker-Build

on:
  push:
    branches: [ main ]
  watch:
    types: [started]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2

    - name: Set up Docker Buildx
      id: buildx
      uses: docker/setup-buildx-action@v2
#      with:
#        version: v0.9.1

    - name: Available platforms
      run: echo ${{ steps.buildx.outputs.platforms }}

    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: DOCKER_USERNAME
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push
      uses: docker/build-push-action@v2
      with:
        context: .
        file: ./Dockerfile
          # 所需要的体系结构，可以在 Available platforms 步骤中获取所有的可用架构
        platforms: linux/amd64,linux/arm64
          # 镜像推送时间
        push: true
          # 给清单打上多个标签
        tags: |
            dswang2233/gocqhttp

#    - name: Build and push arm64
#      uses: docker/build-push-action@v2
#      with:
#        context: .
#        file: ./Dockerfile_arm
#          # 所需要的体系结构，可以在 Available platforms 步骤中获取所有的可用架构
#        platforms: linux/arm64
#          # 镜像推送时间
#        push: true
#          # 给清单打上多个标签
#        tags: |
#            dswang2233/gocqhttp:arm64

#    - name: 创建latest列表
#      run: |
#        docker manifest create dswang2233/gocqhttp \
#          dswang2233/gocqhttp:amd64 \
#          dswang2233/gocqhttp:arm64
        
    # - name: 设置latest列表
    #   run: |
    #     docker manifest annotate dswang2233/gocqhttp \
    #       dswang2233/gocqhttp:arm64 \
    #       --os linux --arch arm64 --variant v8
    #     docker manifest annotate dswang2233/gocqhttp \
    #       dswang2233/gocqhttp:amd64 \
    #       --os linux --arch amd64

#    - name: 推送latest列表
#      run: docker manifest push dswang2233/gocqhttp

  del_runs:
    runs-on: ubuntu-latest
    steps:
      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 7
          keep_minimum_runs: 5
